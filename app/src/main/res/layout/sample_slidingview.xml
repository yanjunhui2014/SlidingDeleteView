<?xml version="1.0" encoding="utf-8"?>
<com.sliding.widget.SlidingDeleteView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/slidingview"
    android:layout_width="match_parent"
    android:layout_height="45dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <!-- 用于正常显示的item -->
        <LinearLayout
            android:id="@+id/lay_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="12dp">

            <TextView
                android:id="@+id/tv_key"
                style="@style/text_black"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/tv_value"
                style="@style/text_black"
                android:layout_marginStart="10dp"
                android:layout_weight="1.0"
                android:textColor="@color/gray_dft" />

            <View
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:background="@mipmap/ic_right_gray" />
        </LinearLayout>

        <!-- 抽屉部分 -->
        <LinearLayout
            android:id="@+id/lay_sliding"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_delete"
                android:layout_width="80dp"
                android:layout_height="match_parent"
                android:background="@drawable/bg_btn_red"
                android:gravity="center"
                android:text="@string/delete"
                android:textColor="@color/white" />
        </LinearLayout>
    </LinearLayout>

</com.sliding.widget.SlidingDeleteView>
